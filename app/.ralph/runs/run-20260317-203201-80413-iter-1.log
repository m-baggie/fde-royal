Working tree is clean. US-001 is complete.

**Summary of what was implemented:**

- `expandQuery(anthropic, q)` async function added to `server/src/routes/assets.js` — calls `claude-haiku-4-5-20251001` with the RC-specific prompt, returns expanded FTS5 OR query, falls back to original `q` on null anthropic or any error
- `GET /api/assets` route made async to await `expandQuery`
- `app.locals.anthropic` initialized in `server/src/index.js` when `ANTHROPIC_API_KEY` is set
- Two Supertest tests added in a `describe('GET /api/assets — query expansion (US-001)')` block
- All 134 tests pass, lint clean, build clean

<promise>COMPLETE</promise>
