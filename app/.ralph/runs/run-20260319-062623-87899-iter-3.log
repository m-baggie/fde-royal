Clean working tree. US-002 is fully verified and committed.

**Summary:**
- **Share button**: renders with correct style (`1.5px solid #001B6B`, transparent bg, `6px 14px` padding, `13px` font)
- **Click behavior**: copies `?asset=<encoded-id>` URL, label flips to `✓ Copied!`, reverts after 2000ms
- **Deep link**: `/?asset=<id>` auto-opens the modal; `?asset=` param removed from URL via `history.replaceState`
- **Quality gates**: `npm run lint` → PASS, `npm run build` → PASS
- **Commit**: `403a4db chore(ralph): add US-002 progress entry for iter-3 re-verification`

<promise>COMPLETE</promise>
